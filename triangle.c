#include <stdio.h>
int main()
{

    int a;
    int h;
    int b;

    printf("Enter the value of h\n");
    scanf("%d", &h);

    printf("Enter the value of b\n");
    scanf("%d", &b);

    printf("your result is %d\n", h * b / 2);

    return 0;
}