#include <stdio.h>
int main()
{

    float c;
    float d;

    printf("Enter the number of c\n");
    scanf("%f", &c);

    printf("Enter the number of d\n");
    scanf("%f", &d);

    printf("the sum of c and d is %f\n", c + d);

    printf("the sub of c and d is %f\n", c - d);

    printf("the mul of c and d is %f\n", c * d);

    printf("the div of c and d is %f\n", c / d);

    // 56.89printf("the mod of c and d is %f\n", c % d);

    return 0;
}